# 이메일 인증 기능 추가 작업 가이드

## 학습 목표

1. **SMTP(Simple Mail Transfer Protocol)**의 개념을 이해하고 Google 메일 서버를 연동할 수 있다.
2. **AJAX (fetch)**를 사용하여 비동기로 메일을 발송하고 응답을 받을 수 있다.
3. **HttpSession**을 활용하여 임시 인증번호를 서버에 저장하고 검증할 수 있다.

---

## 핵심 개념 설명

### 1. SMTP란?
- **Simple Mail Transfer Protocol**: 이메일을 전송하기 위한 프로토콜
- Google의 SMTP 서버(`smtp.gmail.com`)를 사용하여 이메일을 발송
- **포트 587**: TLS 암호화를 사용하는 포트 (보안)

### 2. AJAX (fetch)란?
- **Asynchronous JavaScript And XML**: 비동기 통신 기술
- 페이지 새로고침 없이 서버와 데이터를 주고받을 수 있음
- `fetch()` API: 최신 브라우저에서 제공하는 비동기 통신 함수

### 3. HttpSession이란?
- 서버에 사용자별로 데이터를 저장하는 저장소
- 로그인 정보, 임시 인증번호 등을 저장
- 세션은 서버 메모리에 저장되므로 서버 재시작 시 사라짐

---

## 1단계: 사전 준비 (Google 앱 비밀번호 발급)

> ⚠️ **중요**: 일반 구글 비밀번호로는 SMTP 접근이 불가능합니다!

### 작업 순서:
1. **Google 계정 관리** 접속 → **보안** 탭
2. **2단계 인증**이 켜져 있어야 합니다 (안 켜져 있다면 설정 필요)
3. 검색창에 **"앱 비밀번호"** 검색
4. 앱 이름에 `SpringBootBlog` 입력 후 **만들기**
5. **생성된 16자리 비밀번호**를 복사해서 메모장에 저장해둡니다
   - 예: `abcd efgh ijkl mnop` (띄어쓰기 제거: `abcdefghijklmnop`)
   - ⚠️ 이 비밀번호는 **한 번만 표시**되므로 반드시 저장!

---

## 2단계: 의존성 추가 (build.gradle)

메일 발송 기능을 사용하기 위해 Spring Boot Mail Starter를 추가합니다.

### 작업 내용:
1. `build.gradle` 파일 열기
2. `dependencies` 블록에 다음 코드 추가:

```groovy
dependencies {
    // ... 기존 의존성 유지
    
    // 이메일 발송 기능 추가
    implementation 'org.springframework.boot:spring-boot-starter-mail'
}
```

### 작업 후:
- 반드시 **Load Gradle Changes** (코끼리 아이콘) 클릭!
- Gradle이 의존성을 다운로드하는 동안 잠시 기다립니다.

---

## 3단계: 설정 파일 수정 (application.yml)

Google SMTP 서버 정보를 등록합니다.

### 작업 내용:
1. `src/main/resources/application.yml` 파일 열기
2. `spring:` 섹션에 다음 설정 추가:

```yaml
spring:
  # ... 기존 설정 유지

  # 이메일 설정 추가
  mail:
    host: smtp.gmail.com          # Google SMTP 서버 주소
    port: 587                     # TLS 포트 (보안)
    username: 본인의구글이메일@gmail.com    # ⚠️ 본인 이메일로 변경!
    password: 아까발급받은앱비밀번호16자리  # ⚠️ 앱 비밀번호로 변경! (띄어쓰기 없이)
    properties:
      mail:
        smtp:
          auth: true              # 인증 사용
          starttls:
            enable: true         # TLS 암호화 활성화
            required: true       # TLS 필수
          connectiontimeout: 5000 # 연결 시간 제한 (5초)
          timeout: 5000           # 타임아웃 (5초)
          writetimeout: 5000     # 쓰기 타임아웃 (5초)
```

### ⚠️ 주의사항:
- `username`: 본인의 구글 이메일 주소 입력
- `password`: 1단계에서 발급받은 **앱 비밀번호 16자리** 입력 (띄어쓰기 제거!)
- 일반 구글 비밀번호가 아닌 **앱 비밀번호**여야 합니다!

---

## 4단계: 랜덤 인증번호 생성 유틸 클래스

6자리 랜덤 숫자를 생성하는 유틸리티 클래스를 만듭니다.

### 작업 내용:
1. `src/main/java/org/example/demo_ssr_v1_1/_core/utils/` 폴더에 새 파일 생성
2. 파일명: `MailUtils.java`
3. 다음 코드 입력:

```java
package org.example.demo_ssr_v1_1._core.utils;

import java.util.Random;

/**
 * 메일 발송 유틸리티 클래스
 * 
 * 인증번호 생성 기능을 제공합니다.
 */
public class MailUtils {
    
    /**
     * @return 6자리 랜덤 숫자 문자열 (예: "123456")
     */
    public static String generateRandomCode() {
        Random random = new Random();
        // 0 ~ 899999 사이의 랜덤 숫자 생성  1 + 100_000 = 100_001
        int code = 100_000 + random.nextInt(900_000);
        return String.valueOf(code);
    }
}
```

### 개념 설명:
- `Random.nextInt(900000)`: 0 ~ 899999 사이의 랜덤 숫자 생성
- `100000 + ...`: 최소값 100000을 더하여 100000 ~ 999999 범위로 조정
- `String.valueOf()`: 숫자를 문자열로 변환
- `100_000` 형식: 숫자 가독성을 위한 언더스코어 사용 (Java 7+)

---

## 5단계: 메일 서비스 구현 (핵심 로직)

실제로 메일을 보내고, **세션에 인증번호를 저장**하는 로직입니다.

### 작업 내용:
1. `src/main/java/org/example/demo_ssr_v1_1/user/` 폴더에 새 파일 생성
2. 파일명: `MailService.java`
3. 다음 코드 입력:

```java
package org.example.demo_ssr_v1_1.user;

import jakarta.mail.MessagingException;
import jakarta.mail.internet.MimeMessage;
import jakarta.servlet.http.HttpSession;
import lombok.RequiredArgsConstructor;
import org.example.demo_ssr_v1_1._core.errors.exception.Exception400;
import org.example.demo_ssr_v1_1._core.utils.MailUtils;
import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.mail.javamail.MimeMessageHelper;
import org.springframework.stereotype.Service;

/**
 * 메일 발송 서비스
 * 
 * 비즈니스 로직:
 * 1. 인증번호 생성
 * 2. 이메일 발송
 * 3. 세션에 인증번호 저장 (검증용)
 * 
 * ⚠ 주의: Service 레이어에서 HttpSession을 사용하는 것은 일반적으로 권장되지 않지만,
 * 학습 목적으로 간단하게 구현하기 위해 사용합니다.
 * 실무에서는 Redis 같은 인메모리 DB를 사용합니다.
 * 
 * 핵심 개념:
 * 1. JavaMailSender: Spring Boot가 제공하는 메일 발송 인터페이스
 * 2. MimeMessage: 이메일 메시지를 나타내는 객체 (HTML, 첨부파일 지원)
 * 3. MimeMessageHelper: 이메일 작성 편의 클래스
 * 4. HttpSession: 서버에 임시 데이터 저장 (인증번호 저장용)
 */
@RequiredArgsConstructor
@Service
public class MailService {

    // Spring Boot가 제공하는 메일 발송 객체
    // application.yml에 설정된 SMTP 서버 정보를 사용하여 메일 발송
    private final JavaMailSender javaMailSender;
    
    // 세션 객체 (인증번호 저장용)
    // 일반적으로 Service 레이어에서는 HttpSession을 사용하지 않지만, 학습 목적으로 간단하게 구현하기 위해 사용
    private final HttpSession session;


    /**
     * 인증번호 발송하기
     * @param email - 사용자가 입력한 이메일 주소
     */
    public void 인증번호발송(String email) {
        // 1. 인증번호 생성
        String code = MailUtils.generateRandomCode();
        System.out.println("생성된 인증번호: " + code); // 개발용 로그

        // 2. 이메일 전송 내용 설정
        // MimeMessage  / SimpleMailMessage 도 있음 (순수하게 글자만 보낼 때 사용)
        // - 텍스트뿐만 아니라 HTML, 첨부파일 등을 포함할 수 있는 표준 포맷
        // (백지 편지 봉투) - 우편 위치 부터 정확한 포맷 형식을 맞춰야 함
        MimeMessage message = javaMailSender.createMimeMessage();
        
        try {

            // 3. 도우미 객체(Helper) 생성
            // true: "멀티파트(HTML, 파일 등)를 사용하겠다"는 설정
            MimeMessageHelper helper = new MimeMessageHelper(message, true, "UTF-8");
            
            helper.setTo(email); // 받는 사람 설정
            helper.setSubject("[MyBlog] 회원가입 이메일 인증번호"); // 제목 설정
            // 본문 설정 (HTML 형식)
            helper.setText("<h3>인증번호는 [" + code + "] 입니다.</h3>", true);

            // 4. 메일 발송 (SMTP 서버와 통신)
            // - application.yml에 설정된 구글 SMTP 서버를 통해 전송됨
            javaMailSender.send(message);

            // 5. 세션에 인증번호 저장 (중요!)
            // - 목적: 나중에 사용자가 번호를 입력했을 때 맞는지 확인하기 위해 서버가 기억하고 있어야 함
            // - Key 이름 전략: "code_" + 이메일 (예: "code_test@gmail.com")
            //   -> 동시 접속자가 많아도 이메일 주소로 누구의 인증번호인지 구별할 수 있음
            session.setAttribute("code_" + email, code);
            System.out.println("인증번호 발송 완료: " + code); // 개발용 로그

        } catch (MessagingException e) {
            // 메일 발송 실패 시 예외 처리
            // - SMTP 서버 연결 실패
            // - 인증 실패 (앱 비밀번호 오류 등)
            // - 네트워크 오류
            e.printStackTrace();
            throw new Exception400("메일 발송에 실패했습니다: " + e.getMessage());
        }
    }


    /**
     * [기능 2] 인증번호 검증 메서드
     * * @param email 검증할 이메일 (Key를 찾기 위해 필요)
     * @param code 사용자가 입력한 인증번호
     * @return true(일치), false(불일치)
     */
    public boolean 인증번호확인(String email, String code) {
        // 세션에서 저장된 코드 가져오기
        // Key: "code_" + email (저장할 때와 동일한 키 사용)
        String savedCode = (String) session.getAttribute("code_" + email);

        // 인증번호 비교
        // - savedCode != null: 세션에 인증번호가 저장되어 있음 (세션 만료 안됨)
        // - savedCode.equals(code): 저장된 번호와 사용자가 입력한 번호가 일치
        if (savedCode != null && savedCode.equals(code)) {
            // 인증 성공 시 세션에서 삭제 (일회용)
            // - 같은 인증번호로 다시 인증하는 것을 방지
            // - 메모리 절약
            session.removeAttribute("code_" + email);
            return true;
        }
        
        // 검증 실패
        // - 세션에 인증번호가 없음 (세션 만료 또는 아직 발송 안됨)
        // - 인증번호가 일치하지 않음
        return false;
    }
}
```

### 개념 설명:
- **MimeMessage**: 이메일 메시지를 나타내는 객체
- **MimeMessageHelper**: 이메일 작성 편의 클래스
- **세션 키**: `"code_" + email` 형식으로 저장 (이메일별로 구분)
- **일회용**: 인증 성공 시 세션에서 삭제하여 재사용 방지

---

## 6단계: DTO 추가 (UserRequest)

이메일 인증에 사용할 DTO를 추가합니다.

### 작업 내용:
1. `src/main/java/org/example/demo_ssr_v1_1/user/UserRequest.java` 파일 열기
2. 파일 끝부분 (마지막 `}` 전)에 다음 코드 추가:

```java
    /**
     * 이메일 인증 DTO
     * 
     * 인증번호 발송 및 확인에 사용됩니다.
     */
    @Data
    public static class EmailCheckDTO {
        private String email;  // 이메일 주소
        private String code;   // 인증번호 (확인 시에만 사용)

        /**
         * 유효성 검사
         * 
         * @throws Exception400 이메일이 비어있거나 형식이 올바르지 않을 경우
         */
        public void validate() {
            if (email == null || email.trim().isEmpty()) {
                throw new Exception400("이메일을 입력해주세요");
            }
            if (!email.contains("@")) {
                throw new Exception400("올바른 이메일 형식이 아닙니다");
            }
        }
    }
}
```

### ⚠️ 주의사항:
- `UserRequest` 클래스 내부에 **정적 내부 클래스**로 추가
- 기존 DTO들(`LoginDTO`, `JoinDTO`, `UpdateDTO`)과 같은 위치에 추가

---

## 7단계: API 컨트롤러 생성 (AJAX 요청 처리)

화면 이동 없이 데이터를 주고받기 위해 `@RestController`를 사용합니다.

### 작업 내용:
1. `src/main/java/org/example/demo_ssr_v1_1/user/` 폴더에 새 파일 생성
2. 파일명: `UserApiController.java`
3. 다음 코드 입력:

```java
package org.example.demo_ssr_v1_1.user;

import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

import java.util.Map;

/**
 * 사용자 API 컨트롤러
 * 
 * AJAX 요청을 처리하는 컨트롤러입니다.
 * 
 * @RestController: 
 * - @Controller + @ResponseBody
 * - 반환값을 JSON으로 자동 변환
 * - 화면(View)을 반환하지 않고 데이터만 반환
 */
@RequiredArgsConstructor
@RestController // 데이터만 리턴 (JSON)
public class UserApiController {

    private final MailService mailService;

    /**
     * 인증번호 발송 API
     * @param reqDTO 이메일 주소가 담긴 DTO
     * @return 성공 메시지 (JSON)
     */
    @PostMapping("/api/email/send")
    public ResponseEntity<?> 인증번호발송(@RequestBody UserRequest.EmailCheckDTO reqDTO) {
        // 1. 유효성 검사
        reqDTO.validate();

        // 2. 메일 서비스에 인증번호 발송 요청
        mailService.인증번호발송(reqDTO.getEmail());

        // 3. 성공 응답 반환 (JSON 형식)
        return ResponseEntity.ok().body(Map.of("message", "인증번호가 발송되었습니다."));
    }

    /**
     * 인증번호 확인 API
     * 
     * 처리 과정:
     * 1. DTO 유효성 검사
     * 2. 메일 서비스에 인증번호 확인 요청
     * 3. 성공/실패 응답 반환
     * 
     * @param reqDTO 이메일 주소와 인증번호가 담긴 DTO
     * @return 인증 결과 메시지 (JSON)
     */
    @PostMapping("/api/email/verify")
    public ResponseEntity<?> 인증번호확인(@RequestBody UserRequest.EmailCheckDTO reqDTO) {
        // 1. 유효성 검사
        reqDTO.validate();
        
        if (reqDTO.getCode() == null || reqDTO.getCode().trim().isEmpty()) {
            return ResponseEntity.badRequest().body(Map.of("message", "인증번호를 입력해주세요."));
        }

        // 2. 메일 서비스에 인증번호 확인 요청
        boolean isVerified = mailService.인증번호확인(reqDTO.getEmail(), reqDTO.getCode());

        // 3. 결과에 따른 응답 반환
        if (isVerified) {
            return ResponseEntity.ok().body(Map.of("message", "인증되었습니다."));
        } else {
            return ResponseEntity.badRequest().body(Map.of("message", "인증번호가 일치하지 않습니다."));
        }
    }
}
```

### 개념 설명:
- **@RestController**: JSON 응답을 반환하는 컨트롤러
- **@RequestBody**: HTTP 요청 본문(JSON)을 DTO로 자동 변환
- **ResponseEntity**: HTTP 상태 코드와 본문을 함께 반환
- **Map.of()**: 간단한 JSON 객체 생성 (Java 9+)

---

## 8단계: 뷰 수정 (join-form.mustache)

회원가입 폼에 이메일 인증 기능을 추가합니다.

### 작업 내용:
1. `src/main/resources/templates/user/join-form.mustache` 파일을 **전체** 다음 코드로 **교체**:

```mustache
{{> layout/header}}

<div class="container p-5">
    <div class="card">
        <div class="card-header"><b>회원가입을 해주세요</b></div>
        <div class="card-body">
            <form action="/join" method="post" enctype="multipart/form-data" onsubmit="return checkVerify()">
                <!-- 사용자명 입력 -->
                <div class="mb-3">
                    <input type="text" class="form-control" placeholder="Enter username" name="username" required>
                </div>
                
                <!-- 비밀번호 입력 -->
                <div class="mb-3">
                    <input type="password" class="form-control" placeholder="Enter password" name="password" required>
                </div>
                
                <!-- 이메일 입력 및 인증번호 발송 -->
                <div class="mb-3">
                    <label class="form-label">이메일</label>
                    <div class="input-group">
                        <input type="email" class="form-control" id="email" name="email" placeholder="이메일 입력" required>
                        <button type="button" class="btn btn-outline-secondary" onclick="callSendApi()">인증번호 전송</button>
                    </div>
                </div>
                
                <!-- 인증번호 입력 (처음에는 숨김) -->
                <div class="mb-3" id="code-box" style="display:none;">
                    <label class="form-label">인증번호</label>
                    <div class="input-group">
                        <input type="text" class="form-control" id="code" placeholder="인증번호 6자리" maxlength="6">
                        <button type="button" class="btn btn-outline-secondary" onclick="callVerifyApi()">확인</button>
                    </div>
                    <!-- 인증 결과 메시지 표시 영역 -->
                    <div id="msg" class="form-text"></div>
                </div>
                
                <!-- 인증 완료 여부 저장 (hidden 필드) -->
                <input type="hidden" id="isEmailVerified" value="false">
                
                <!-- 프로필 이미지 업로드 (선택사항) -->
                <div class="mb-3">
                    <label for="profileImage" class="form-label">프로필 사진 (선택사항)</label>
                    <input type="file" class="form-control" id="profileImage" name="profileImage" accept="image/*">
                    <small class="form-text text-muted">이미지 파일만 업로드 가능합니다 (JPG, PNG, GIF 등)</small>
                    <small class="form-text text-muted d-block mt-1">💡 프로필 사진을 등록하지 않아도 회원가입이 가능합니다.</small>
                </div>
                <!-- 회원가입 버튼 -->
                <button type="submit" class="btn btn-primary form-control">회원가입</button>
            </form>
        </div>
    </div>
</div>

{{> layout/footer}}

<script>

    // 이메일 전송 API 호출 (AJAX)
    async function callSendApi() {
        let email = document.querySelector("#email").value;

        // 이메일 입력 확인
        if (!email) {
            alert("이메일을 입력해주세요.");
            return;
        }

        try {
            // fetch API로 POST 요청
            let response = await fetch("/api/email/send", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json"  // JSON 형식으로 전송
                },
                body: JSON.stringify({ email: email })  // 객체를 JSON 문자열로 변환
            });

            // 응답 처리
            if (response.ok) {
                alert("인증번호가 전송되었습니다! 메일함을 확인하세요.");
                document.querySelector("#code-box").style.display = "block"; // 인증번호 입력창 보이기
            } else {
                // 서버에서 에러 응답 (400, 500 등)
                let error = await response.json();
                alert("실패: " + (error.message || "메일 발송 실패"));
            }
        } catch (e) {
            console.log(e);
            alert("서버 통신 오류: " + e.message);
        }
    }


    // 인증번호 확인 API 호출 (AJAX)
    async function callVerifyApi() {
        let email = document.querySelector("#email").value;
        let code = document.querySelector("#code").value;

        // 인증번호 입력 확인
        if (!code) {
            alert("인증번호를 입력해주세요.");
            return;
        }

        try {
            // fetch API로 POST 요청
            let response = await fetch("/api/email/verify", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json"
                },
                body: JSON.stringify({ email: email, code: code })
            });

            let result = await response.json();
            let msgBox = document.querySelector("#msg");

            if (response.ok) {
                // 인증 성공
                msgBox.innerHTML = "<span style='color:green'> 인증되었습니다.</span>";
                document.querySelector("#isEmailVerified").value = "true"; // 인증 완료 상태 저장
                document.querySelector("#email").readOnly = true; // 이메일 수정 불가 처리
            } else {
                // 인증 실패
                msgBox.innerHTML = "<span style='color:red'> 인증번호가 틀렸습니다.</span>";
                document.querySelector("#isEmailVerified").value = "false";
            }
        } catch (e) {
            console.log(e);
            alert("오류 발생: " + e.message);
        }
    }

    // 1. 회원가입 폼 제출 시 최종 체크
    // 2. 중요: form의 onsubmit 이벤트에서 호출됨
    // 2.1 이메일 인증을 완료하지 않으면 폼 제출을 막음 (return false)
    // 2.2 인증이 완료되었으면 폼 제출 허용 (return true)
    function checkVerify() {
        let isVerified = document.querySelector("#isEmailVerified").value;
        if (isVerified !== "true") {
            alert("이메일 인증을 먼저 완료해주세요.");
            return false; // 폼 제출 막기
        }
        return true; // 폼 제출 허용 (일반 form submit 진행)
    }
</script>
```

### 개념 설명:

#### 1. Form 제출 제어 (onsubmit)
- **onsubmit="return checkVerify()"**: 폼 제출 전에 함수 실행
- `return false`: 폼 제출을 **막음** (인증 미완료 시)
- `return true`: 폼 제출을 **허용** (인증 완료 시)
- ⚠️ **중요**: `return`을 반드시 붙여야 제출이 막힙니다!

#### 2. AJAX 통신 (fetch API)
- **async/await**: 비동기 함수를 동기처럼 작성
- **fetch()**: 브라우저 내장 API, AJAX 요청을 보냄
- **JSON.stringify()**: JavaScript 객체를 JSON 문자열로 변환
- **response.ok**: HTTP 상태 코드가 200~299 범위인지 확인

#### 3. 인증 상태 관리
- **hidden 필드 (`isEmailVerified`)**: 인증 완료 여부를 저장
- **readOnly**: 이메일 인증 후 수정 방지
- 인증 성공 시 `isEmailVerified` 값을 `"true"`로 변경

#### 4. 왜 일반 form submit을 사용하나요?
- 프로필 이미지 파일 업로드가 포함되어 있음
- `multipart/form-data` 형식으로 전송해야 함
- 기존 `UserController.joinProc()` 메서드가 form 데이터를 받도록 구현되어 있음
- 따라서 AJAX가 아닌 **일반 form submit**을 사용하되, 제출 전에 인증 체크만 수행

---

## 9단계: 테스트

### 테스트 순서:
1. **애플리케이션 실행**
2. **회원가입 페이지 접속**: `http://localhost:8080/join`
3. **이메일 입력 후 "인증번호 전송" 버튼 클릭**
4. **메일함 확인** (스팸 메일함도 확인!)
5. **인증번호 입력 후 "확인" 버튼 클릭**
6. **인증 완료 메시지 확인**
7. **회원가입 진행**

### 예상 결과:
- ✅ 인증번호가 메일로 발송됨
- ✅ 인증번호 입력창이 나타남
- ✅ 인증 성공 시 "✓ 인증되었습니다." 메시지 표시
- ✅ 이메일 입력창이 수정 불가 상태로 변경됨
- ✅ 인증 완료 후 회원가입 가능

---

## Q&A 대비

### 1. "메일이 안 와요!"

**가능한 원인:**
- **스팸 메일함** 확인 (가장 흔한 원인!)
- 학교/공공장소 와이파이에서 SMTP 포트(587)가 막혀있을 수 있음
- `application.yml`의 비밀번호가 **앱 비밀번호**인지 확인
- 일반 구글 비밀번호가 아닌 **앱 비밀번호 16자리**여야 함

**해결 방법:**
1. 스팸 메일함 확인
2. 다른 네트워크에서 테스트
3. `application.yml` 설정 재확인

### 2. "세션에 저장하면 서버 꺼지면 사라지나요?"

**답변:**
- 네, 맞습니다. 세션은 서버 메모리에 저장되므로 서버를 재시작하면 사라집니다.
- "원래 실무에서는 **Redis**라는 인메모리 DB를 사용해서 5분 제한시간을 두고 저장하지만, 우리는 배우는 단계라 가장 쉬운 세션을 활용했다"라고 설명

### 3. "인증번호 유효시간은 어떻게 되나요?"

**답변:**
- 현재 구현: 세션 유효시간(기본 30분)을 따름
- 실무에서는 보통 3~5분으로 제한
- **과제**: "인증번호 유효시간 3분 제한 기능을 자바로 구현해보기"
  - 세션에 `code`뿐만 아니라 `createTime`도 같이 저장
  - 확인 시 현재 시간과 비교하여 3분 이내인지 체크

### 4. "왜 Service에서 HttpSession을 사용하나요?"

**답변:**
- 일반적으로 Service 레이어는 비즈니스 로직에만 집중해야 하므로 HttpSession을 사용하지 않는 것이 좋습니다.
- 하지만 학습 목적으로 간단하게 구현하기 위해 사용했습니다.
- 실무에서는 Controller에서 세션을 관리하거나, Redis 같은 별도 저장소를 사용합니다.

### 5. "AJAX가 뭔가요?"

**답변:**
- **Asynchronous JavaScript And XML**: 비동기 통신 기술
- 페이지를 새로고침하지 않고 서버와 데이터를 주고받을 수 있음
- 예: 인증번호 전송 버튼을 눌러도 페이지가 새로고침되지 않고, 메일만 발송됨
- `fetch()` API는 최신 브라우저에서 제공하는 AJAX 통신 함수

---

## 체크리스트

작업 완료 여부를 확인하세요:

- [ ] Google 앱 비밀번호 발급 완료
- [ ] `build.gradle`에 `spring-boot-starter-mail` 의존성 추가
- [ ] `application.yml`에 메일 설정 추가 (username, password 확인!)
- [ ] `MailUtils.java` 클래스 작성 완료
- [ ] `MailService.java` 클래스 작성 완료
- [ ] `UserRequest.EmailCheckDTO` 추가 완료
- [ ] `UserApiController.java` 생성 완료
- [ ] `join-form.mustache` 수정 완료 (이메일 인증 UI + JavaScript)
- [ ] 테스트 완료 (실제 메일 발송 확인)
- [ ] 인증번호 확인 기능 테스트 완료
- [ ] 회원가입 시 인증 체크 기능 테스트 완료

---

## 추가 개선 과제 (선택사항)

### 과제 1: 인증번호 유효시간 3분 제한
- 세션에 인증번호와 함께 생성 시간(`createTime`) 저장
- 확인 시 현재 시간과 비교하여 3분 이내인지 체크
- 3분 초과 시 "인증번호가 만료되었습니다" 메시지 표시

### 과제 2: 인증번호 재발송 기능
- 인증번호를 받지 못했을 때 재발송 버튼 추가
- 재발송 시 기존 인증번호는 무효화

---

## 10단계: 이메일 중복 가입 방지 기능 추가

회원가입 시 이메일 중복을 방지하는 기능을 추가합니다.

### 작업 내용:

#### 1. UserRepository에 이메일 조회 메서드 추가

`src/main/java/org/example/demo_ssr_v1_1/user/UserRepository.java` 파일에 다음 메서드 추가:

```java
/**
 * 이메일로 사용자 조회
 * 
 * 쿼리 메서드 네이밍:
 * - findBy: 조회 시작
 * - Email: 엔티티의 email 필드명과 일치
 * - Optional<User>: 결과가 없을 수 있으므로 Optional로 반환
 * 
 * 자동 생성되는 SQL:
 * SELECT * FROM user_tb WHERE email = ?
 */
Optional<User> findByEmail(String email);
```

#### 2. UserService에 이메일 중복 체크 추가

`src/main/java/org/example/demo_ssr_v1_1/user/UserService.java` 파일의 `회원가입()` 메서드에서 사용자명 중복 체크 다음에 추가:

```java
// 2-1. 이메일 중복 체크
// 회원가입 시 이메일이 이미 등록되어 있는지 확인
if (userRepository.findByEmail(joinDTO.getEmail()).isPresent()) {
    throw new Exception400("이미 등록된 이메일입니다");
}
```

#### 3. User 엔티티에 유니크 제약조건 추가

`src/main/java/org/example/demo_ssr_v1_1/user/User.java` 파일의 email 필드에 유니크 제약조건 추가:

```java
@Column(unique = true)
private String email;
```

#### 4. 예외 처리 개선 (MyExceptionHandler)

`src/main/java/org/example/demo_ssr_v1_1/_core/errors/MyExceptionHandler.java` 파일 수정:

**Exception400 핸들러를 alert 창으로 변경:**

```java
@ExceptionHandler(Exception400.class)
@ResponseBody
public ResponseEntity<String> ex400(Exception400 e, HttpServletRequest request) {
    log.warn("=== 400 에러 발생  ===");
    log.warn("요청 URL : {}", request.getRequestURL());
    log.warn("에러 메세지 : {}", e.getMessage());
    log.warn("예외 클래스 : {}", e.getClass().getSimpleName());
    
    // 메시지의 작은따옴표를 이스케이프 처리
    String message = e.getMessage() != null ? e.getMessage() : "잘못된 요청입니다";
    String escapedMessage = message.replace("'", "\\'");
    String script = "<script>alert('" + escapedMessage + "');" +
            "history.back();" +
            "</script>";

    return ResponseEntity
            .status(HttpStatus.BAD_REQUEST)
            .contentType(MediaType.TEXT_HTML)
            .body(script);
}
```

**DataIntegrityViolationException 핸들러 개선 (유니크 제약조건 위반 처리):**

```java
@ExceptionHandler(DataIntegrityViolationException.class)
@ResponseBody
public ResponseEntity<String> handleDataIntegrityViolationException(DataIntegrityViolationException e,
                                                     HttpServletRequest request) {
    log.warn("=== 데이터베이스 제약조건 위반 오류 발생  ===");
    log.warn("요청 URL : {}", request.getRequestURL());
    log.warn("에러 메세지 : {}", e.getMessage());
    log.warn("예외 클래스 : {}", e.getClass().getSimpleName());
    
    String errorMessage = e.getMessage();
    String userMessage;
    
    // 유니크 제약조건 위반 (중복 데이터)
    if (errorMessage != null) {
        if (errorMessage.contains("username") || errorMessage.contains("UK_") && errorMessage.contains("username")) {
            userMessage = "이미 존재하는 사용자 이름입니다";
        } else if (errorMessage.contains("email") || errorMessage.contains("UK_") && errorMessage.contains("email")) {
            userMessage = "이미 등록된 이메일입니다";
        } else if (errorMessage.contains("FOREIGN KEY")) {
            userMessage = "관련된 데이터가 있어 삭제할 수 없습니다. (예: 게시글에 댓글이 있는 경우)";
        } else {
            userMessage = "데이터베이스 제약조건 위반이 발생했습니다";
        }
    } else {
        userMessage = "데이터베이스 제약조건 위반이 발생했습니다";
    }
    
    // 메시지의 작은따옴표를 이스케이프 처리
    String escapedMessage = userMessage.replace("'", "\\'");
    String script = "<script>alert('" + escapedMessage + "');" +
            "history.back();" +
            "</script>";

    return ResponseEntity
            .status(HttpStatus.BAD_REQUEST)
            .contentType(MediaType.TEXT_HTML)
            .body(script);
}
```

### 개념 설명:

#### 1. 이중 방어 체계
- **애플리케이션 레벨 (1차 방어)**: `UserService`에서 이메일 중복 체크
  - 빠른 피드백 제공
  - 사용자 경험 향상
- **데이터베이스 레벨 (2차 방어)**: `@Column(unique = true)`로 DB 제약조건 설정
  - 동시성 문제 방지
  - 데이터 무결성 보장

#### 2. 동시성 문제 예시
두 사용자가 동시에 같은 이메일로 가입 시도 시:
- 애플리케이션 체크만으로는 둘 다 통과할 수 있음
- DB 유니크 제약조건으로 한 명만 저장되고, 나머지는 `DataIntegrityViolationException` 발생
- 예외 핸들러에서 alert 창으로 "이미 등록된 이메일입니다" 메시지 표시

#### 3. 예외 처리 흐름
1. `UserService`에서 `Exception400` 발생 → `MyExceptionHandler.ex400()` 처리 → alert 창 표시
2. DB 제약조건 위반 시 `DataIntegrityViolationException` 발생 → `MyExceptionHandler.handleDataIntegrityViolationException()` 처리 → alert 창 표시

### 수정된 파일 목록:
- `UserRepository.java` - `findByEmail()` 메서드 추가
- `UserService.java` - 이메일 중복 체크 로직 추가
- `User.java` - email 필드에 `@Column(unique = true)` 추가
- `MyExceptionHandler.java` - Exception400과 DataIntegrityViolationException을 alert 창으로 처리

---

## 완료!

이제 이메일 인증 기능과 이메일 중복 가입 방지 기능이 완성되었습니다! 🎉

각 단계를 차근차근 따라하면서 개념을 이해하고, 테스트를 통해 동작을 확인해보세요.

