# 주요 변경사항 정리

## 이메일 인증 기능 추가

### 작업 순서

1. **의존성 추가** (`build.gradle`)
   - `spring-boot-starter-mail` 추가

2. **설정 파일 수정** (`application.yml`)
   - Google SMTP 서버 설정 추가

3. **유틸 클래스 생성** (`MailUtils.java`)
   - 6자리 랜덤 인증번호 생성

4. **메일 서비스 구현** (`MailService.java`)
   - 인증번호 발송 및 확인 기능
   - 세션에 인증번호 저장

5. **API 컨트롤러 생성** (`UserApiController.java`)
   - `/api/email/send`: 인증번호 발송 API
   - `/api/email/verify`: 인증번호 확인 API

6. **DTO 추가** (`UserRequest.EmailCheckDTO`)
   - 이메일 인증용 DTO

7. **뷰 수정** (`join-form.mustache`)
   - 이메일 인증 UI 추가
   - AJAX 통신을 위한 JavaScript 추가
   - 폼 제출 전 인증 체크 기능

### 수정된 파일 목록

- `build.gradle` - 메일 의존성 추가
- `application.yml` - SMTP 설정 추가
- `src/main/java/org/example/demo_ssr_v1_1/_core/utils/MailUtils.java` - 신규 생성
- `src/main/java/org/example/demo_ssr_v1_1/user/MailService.java` - 신규 생성
- `src/main/java/org/example/demo_ssr_v1_1/user/UserApiController.java` - 신규 생성
- `src/main/java/org/example/demo_ssr_v1_1/user/UserRequest.java` - EmailCheckDTO 추가
- `src/main/resources/templates/user/join-form.mustache` - 이메일 인증 기능 추가

### 핵심 기능

- **인증번호 발송**: Google SMTP를 통한 이메일 발송
- **인증번호 확인**: 세션에 저장된 인증번호와 비교
- **AJAX 통신**: 페이지 새로고침 없이 인증 처리
- **폼 제출 제어**: 이메일 인증 완료 후에만 회원가입 가능

---

## 이메일 중복 가입 방지 기능 추가

### 작업 순서

1. **UserRepository에 이메일 조회 메서드 추가**
   - `findByEmail(String email)` 메서드 추가

2. **UserService에 이메일 중복 체크 추가**
   - `회원가입()` 메서드에서 이메일 중복 체크 로직 추가

3. **User 엔티티에 유니크 제약조건 추가**
   - `email` 필드에 `@Column(unique = true)` 추가

4. **예외 처리 개선**
   - `MyExceptionHandler`에서 `Exception400`을 alert 창으로 처리
   - `DataIntegrityViolationException` 핸들러 개선 (유니크 제약조건 위반 처리)

### 수정된 파일 목록

- `src/main/java/org/example/demo_ssr_v1_1/user/UserRepository.java` - `findByEmail()` 메서드 추가
- `src/main/java/org/example/demo_ssr_v1_1/user/UserService.java` - 이메일 중복 체크 로직 추가
- `src/main/java/org/example/demo_ssr_v1_1/user/User.java` - email 필드에 유니크 제약조건 추가
- `src/main/java/org/example/demo_ssr_v1_1/_core/errors/MyExceptionHandler.java` - 예외 처리 개선

### 핵심 기능

- **이중 방어 체계**: 애플리케이션 레벨 + 데이터베이스 레벨 중복 방지
- **동시성 문제 해결**: DB 유니크 제약조건으로 동시 가입 시도 방지
- **사용자 친화적 에러 처리**: alert 창으로 명확한 오류 메시지 표시
